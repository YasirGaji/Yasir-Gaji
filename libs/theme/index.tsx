import { extendTheme } from "@chakra-ui/react";
import {
  config,
  styles,
  componenets,
  fonts,
  colors,
} from "./styles/theme";


export const Theme = extendTheme({
  config,
  styles,
  componenets,
  fonts,
  colors,
});